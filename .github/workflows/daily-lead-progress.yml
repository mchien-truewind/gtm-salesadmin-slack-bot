name: Daily Lead Progress

on:
  schedule:
    # Two UTC schedules for DST handling.
    # Daily report for local Monday-Saturday at ~6 PM PT.
    # (UTC Tue-Sun because local PT date is previous day for 01/02 UTC.)
    - cron: "7 1 * * 0,2-6"
    - cron: "7 2 * * 0,2-6"
    # Weekly report for local Sunday at ~11:59 PM PT (UTC Monday).
    - cron: "57 6 * * 1"
    - cron: "57 7 * * 1"
  workflow_dispatch:
    inputs:
      target_channel:
        description: "Optional channel name override (e.g., slack-testing)"
        required: false
        type: string
      dry_run:
        description: "Compute only; do not post"
        required: false
        default: false
        type: boolean

permissions:
  contents: read

jobs:
  post-progress:
    runs-on: ubuntu-latest
    concurrency:
      group: daily-lead-progress
      cancel-in-progress: false
    env:
      SLACK_USER_TOKEN: ${{ secrets.SLACK_USER_TOKEN }}
      LEAD_REPORT_TARGET_CHANNEL: ${{ vars.LEAD_REPORT_TARGET_CHANNEL || 'gtm-general' }}
      LEAD_REPORT_INBOUND_CHANNEL: ${{ vars.LEAD_REPORT_INBOUND_CHANNEL || 'leads' }}
      LEAD_REPORT_OUTBOUND_CHANNEL: ${{ vars.LEAD_REPORT_OUTBOUND_CHANNEL || 'gtm-outbound' }}
      LEAD_REPORT_INBOUND_PHRASE: ${{ vars.LEAD_REPORT_INBOUND_PHRASE || 'Booked Calendly Meeting' }}
      LEAD_REPORT_OUTBOUND_PHRASE: ${{ vars.LEAD_REPORT_OUTBOUND_PHRASE || 'New Meeting' }}
      LEAD_REPORT_TIMEZONE: ${{ vars.LEAD_REPORT_TIMEZONE || 'America/Los_Angeles' }}
      LEAD_REPORT_FORCE_MODE: ${{ steps.gate.outputs.report_mode || '' }}
      INPUT_TARGET_CHANNEL: ${{ github.event.inputs.target_channel || '' }}
      INPUT_DRY_RUN: ${{ github.event.inputs.dry_run || 'false' }}
      INPUT_SCHEDULE: ${{ github.event.schedule || '' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Schedule Gate (Mode Selection)
        id: gate
        shell: bash
        run: |
          if [[ "${GITHUB_EVENT_NAME}" != "schedule" ]]; then
            echo "should_run=true" >> "$GITHUB_OUTPUT"
            echo "report_mode=" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          case "${INPUT_SCHEDULE}" in
            "7 1 * * 0,2-6"|"7 2 * * 0,2-6")
              echo "should_run=true" >> "$GITHUB_OUTPUT"
              echo "report_mode=daily" >> "$GITHUB_OUTPUT"
              ;;
            "57 6 * * 1"|"57 7 * * 1")
              echo "should_run=true" >> "$GITHUB_OUTPUT"
              echo "report_mode=weekly" >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "should_run=false" >> "$GITHUB_OUTPUT"
              echo "report_mode=" >> "$GITHUB_OUTPUT"
              echo "Skipping run due to unknown schedule expression: ${INPUT_SCHEDULE}"
              ;;
          esac

      - name: Run Daily Progress Post
        if: steps.gate.outputs.should_run == 'true'
        shell: bash
        run: |
          set -euo pipefail

          args=()
          if [[ -n "${INPUT_TARGET_CHANNEL}" ]]; then
            args+=(--target-channel "${INPUT_TARGET_CHANNEL}")
          fi
          if [[ "${INPUT_DRY_RUN}" == "true" ]]; then
            args+=(--dry-run)
          fi

          python3 scripts/slack/post_daily_progress.py "${args[@]}"
